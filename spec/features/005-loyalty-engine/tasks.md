# Tareas: Motor de Lealtad (005-loyalty-engine)

- `[ ]` 1. Generar módulo, controlador y servicio `loyalty-engine`.
- `[ ]` 2. Crear DTO `ScanPassDto`.
- `[ ]` 3. Implementar lógica `PATCH` en el servicio usando tokens de autenticación de Google.
- `[ ]` 4. Configurar controlador para consumir el servicio.
- `[ ]` 5. Verificar compilación con `npm run build`.
